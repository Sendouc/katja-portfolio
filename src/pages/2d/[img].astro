---
import Layout from "../../components/Layout.astro"
import images from "../../data/images.json"

export async function getStaticPaths() {
  return images["2d"].map(img => ({ params: {img}}));
}

const { img } = Astro.request.params;
---

<style>
  a {
    display: block;
    text-align: center;
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  img {
    text-align: center;
    max-width: 100vw;
  }

  div {
    display: flex;
    justify-content: center;
  }
</style>

<Layout activeLink="2d">
  <a href="/2d">← Back to gallery</a>
  <div>
    <img src={`/img/2d/${img}`} />
  </div>
</Layout>
